import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imageSize: {
    width: 120,
    height: 125,
  },
  containerStyles: {
    alignItems: "center",
  },
  buttonContainer: {
    flex: 1,
    justifyContent: "flex-end",
  },
});
